// Single source of truth for gallery card dimensions and shared styles
export const CARD_WIDTH = 200;
export const CARD_HEIGHT = 260;
export const CARD_GAP = 20; // space between cards in the carousel
export const CARD_RADIUS = "var(--radius-lg)"; // 12px

// Featured banner = two cards side by side + gap
export const FEATURED_WIDTH = CARD_WIDTH * 2 + CARD_GAP; // 420px
export const FEATURED_HEIGHT = CARD_HEIGHT; // same height as cards
